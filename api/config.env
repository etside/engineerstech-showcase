; engineersTech Production Configuration
; Domain: biz.engineerstechbd.com

DB_HOST = localhost
DB_NAME = samarain_engineerstech
DB_USER = samarain_et
DB_PASS = Pjokjict4@#

; JWT secret — unique per domain
JWT_SECRET = et-biz-engineerstechbd-jwt-s3cr3t-2026-samarain-mcp-prod

; Canonical site URL — used for OAuth issuer/audience and all absolute URLs
SITE_URL = https://biz.engineerstechbd.com

; Upload directory (relative to api/)
UPLOAD_DIR = ../uploads/

; SMTP (configure in cPanel or leave blank to use PHP mail())
SMTP_HOST = localhost
SMTP_PORT = 587
SMTP_USER =
SMTP_PASS =
SMTP_FROM = no-reply@engineerstechbd.com
